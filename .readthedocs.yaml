version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

sphinx:
  builder: html
  configuration: docs/conf.py

formats:
  - pdf

python:
  install:
    - method: uv
      command: sync
      groups:
        - all
